# range() generates a series of numbers
for value in range(1, 5):
    print(value)
    
# range(inclusive, exclusive, optional step size)

# Can be used to populate a list
# Using the list function
numbers = list(range(1, 10))
print(numbers)